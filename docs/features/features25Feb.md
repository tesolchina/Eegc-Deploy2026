allow teachers to access students' unique IDs (once student registered) 
consider how to handle duplicates. 

remove model selection 
use GPT-5.2 by default if affordable ... 
monitor token usage in the super admin and teacher dashboard 

allow users to adjust the widths of the two panes (left for chat and right for revision)

adjust the prompt so the AI response would be more concise and targeted 

